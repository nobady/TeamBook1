<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:binding="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View"/>
        <variable
            name="mLoginViewModel"
            type="com.loufei.teambook.ui.login.LoginViewModel" />
    </data>


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/green_50">


        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/aiv_app_icon"
            android:layout_width="@dimen/height_normal"
            android:layout_height="@dimen/height_normal"
            android:layout_marginTop="@dimen/margin_large"
            android:src="@mipmap/login_icon"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


        <include
            android:visibility="@{mLoginViewModel.uiState.showLogin?View.VISIBLE:View.GONE}"
            android:id="@+id/layout_username"
            layout="@layout/layout_username_login"
            android:layout_marginLeft="@dimen/margin_normal"
            android:layout_marginRight="@dimen/margin_normal"
            app:layout_constraintTop_toBottomOf="@id/aiv_app_icon"
            android:layout_marginTop="@dimen/margin_large"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            />

        <include
            android:visibility="@{mLoginViewModel.uiState.showLogin?View.GONE:View.VISIBLE}"
            android:id="@+id/layout_pwd"
            layout="@layout/layout_pwd_login"
            android:layout_marginLeft="@dimen/margin_normal"
            android:layout_marginRight="@dimen/margin_normal"
            app:layout_constraintTop_toBottomOf="@id/aiv_app_icon"
            android:layout_marginTop="@dimen/margin_large"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>